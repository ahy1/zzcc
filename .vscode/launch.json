{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CodeLLDB: Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/zzcpp",
            "args": ["${workspaceFolder}/tests/testcpp_ifndef.c"],
            "cwd": "${fileDirname}"
        }
    ]
}
